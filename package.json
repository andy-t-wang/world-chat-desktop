{
  "name": "world-chat-desktop",
  "version": "1.6.1",
  "description": "World Chat Desktop - Secure encrypted messaging",
  "main": "dist/main.js",
  "author": {
    "name": "World",
    "email": "support@world.org"
  },
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"pnpm run dev:webapp\" \"pnpm run dev:electron\"",
    "dev:webapp": "cd webapp && pnpm dev",
    "dev:electron": "wait-on http://localhost:3000 && tsc && electron .",
    "build": "cd webapp && pnpm build && cd .. && tsc",
    "package": "pnpm build && electron-builder",
    "package:mac": "pnpm build && electron-builder --mac",
    "package:win": "pnpm build && electron-builder --win",
    "package:linux": "pnpm build && electron-builder --linux",
    "postinstall": "cd webapp && pnpm install"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "typescript": "^5.0.0",
    "wait-on": "^8.0.0"
  },
  "build": {
    "appId": "com.world.chat.desktop",
    "productName": "World Chat",
    "directories": {
      "output": "release"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "andy-t-wang",
        "repo": "world-chat-desktop",
        "releaseType": "release"
      },
      {
        "provider": "s3",
        "bucket": "world-chat-distribution",
        "endpoint": "https://49b69457481a0d6bacbbd10621d19c50.r2.cloudflarestorage.com",
        "region": "auto",
        "acl": null,
        "path": "/"
      }
    ],
    "files": [
      "dist/**/*",
      "webapp/.next/**/*",
      "webapp/public/**/*",
      "webapp/node_modules/**/*",
      "webapp/package.json"
    ],
    "asarUnpack": [
      "dist/translation-worker.js",
      "node_modules/**/*"
    ],
    "artifactName": "${productName}-${version}-${arch}-${os}.${ext}",
    "mac": {
      "category": "public.app-category.social-networking",
      "artifactName": "WorldChat-${version}-${arch}-mac.${ext}",
      "target": [
        {
          "target": "dmg",
          "arch": ["x64", "arm64"]
        },
        {
          "target": "zip",
          "arch": ["x64", "arm64"]
        }
      ],
      "icon": "assets/icon.icns",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "entitlements": "build/entitlements.mac.plist",
      "entitlementsInherit": "build/entitlements.mac.plist",
      "notarize": true
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": ["x64"]
        }
      ],
      "artifactName": "WorldChat-${version}-${arch}-win.${ext}",
      "icon": "assets/icon-1024.png"
    },
    "linux": {
      "target": [
        {
          "target": "AppImage",
          "arch": ["x64"]
        },
        {
          "target": "deb",
          "arch": ["x64"]
        }
      ],
      "artifactName": "WorldChat-${version}-${arch}-linux.${ext}",
      "icon": "assets/icon-1024.png",
      "category": "Network;Chat"
    },
    "extraMetadata": {
      "main": "dist/main.js"
    }
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "electron-updater": "^6.7.3",
    "recharts": "^3.6.0"
  }
}
